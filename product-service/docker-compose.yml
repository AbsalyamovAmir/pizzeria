version: '1.0'

services:

  postgres:
    image: postgres:11.13
    container_name: product-service
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_DB=pizza_product
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=0000